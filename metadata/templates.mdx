---
title: 'Templates'
description: 'Templates'
icon: 'map'
---

A template is a stored **task pipeline** that can be used to replicate task behaviour for a task.

Templates are scoped to an organization and can be referenced in the creation of tasks (in the metadata) instead of defining the pipeline manually.