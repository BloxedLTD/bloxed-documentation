---
title: 'Templates'
description: 'Templates'
icon: 'map'
---

A template is a stored **task pipeline** that can be used to replicate task behaviour for either a task or a job.
Templates are scoped to an organization and can be referenced in the creation of tasks instead of defining manually.